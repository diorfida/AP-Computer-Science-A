
/**
 * Write a description of class test here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class test
{
    public static void main(){
        String word = "letsgonation";
        
        int length = word.length();
        System.out.println("The last two characters in that word are " + word.substring(length - 2));
    }
}
